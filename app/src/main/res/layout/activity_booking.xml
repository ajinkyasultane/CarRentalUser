<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Car Image -->
        <ImageView
            android:id="@+id/booking_car_image"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop" />

        <!-- Car Name -->
        <TextView
            android:id="@+id/booking_car_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Car Name"
            android:textStyle="bold"
            android:textSize="18sp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp" />

        <!-- Start Date -->
        <EditText
            android:id="@+id/booking_start_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Start Date (e.g. 2025-05-12)"
            android:inputType="date" />

        <!-- End Date -->
        <EditText
            android:id="@+id/booking_end_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="End Date (e.g. 2025-05-14)"
            android:inputType="date" />

        <!-- Pickup Location -->
        <EditText
            android:id="@+id/booking_location"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Pickup Location"
            android:inputType="text" />

        <!-- Confirm Booking Button -->
        <Button
            android:id="@+id/btn_confirm_booking"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Confirm Booking"
            android:layout_marginTop="16dp" />

    </LinearLayout>
</ScrollView>
